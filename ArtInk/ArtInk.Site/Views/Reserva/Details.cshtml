@model ArtInk.Site.ViewModels.Response.ReservaResponseDTO

@{
    ViewData["Title"] = "Detalle Reserva";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Detalle Reserva"]</title>
    <style>
        .small-content {
            font-size: 1rem;
        }

            .small-content h1 {
                font-size: 1.5rem;
            }

            .small-content th, .small-content td {
                padding: 0.5rem;
            }

            .small-content .btn {
                font-size: 0.8rem;
                padding: 0.25rem 1rem;
            }
    </style>
</head>

<body>
    <div class="container fade-in mt-7 mb-7 small-content">
       

        <table class="table table-striped table-dark table-hover">
            <thead>
                <tr>
                    <th colspan="2" class="bg-primary text-white text-center">Detalle Producto</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Id)</th>
                    <td>@Html.DisplayFor(model => model.Id)</td>
                </tr>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.IdSucursalHorario)</th>
                    <td>@Html.DisplayFor(model => model.IdSucursalHorario)</td>
                </tr>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Estado)</th>
                    <td>@Html.DisplayFor(model => model.Estado)</td>
                </tr>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Activo)</th>
                    <td>@Html.DisplayFor(model => model.Activo)</td>
                </tr>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Hora)</th>
                    <td>@Html.DisplayFor(model => model.Hora)</td> 
                </tr>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Fecha)</th>
                    <td>@Html.DisplayFor(model => model.Fecha)</td>
                </tr>
            </tbody>
        </table>

        <div class="mt-3">
            <a href="@Url.Action("Edit", new { id = Model.Id })" class="btn btn-primary me-2">Editar</a>
            <a href="@Url.Action("Index")" class="btn btn-secondary">Regresar</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

